<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:label="kill vip"
        android:icon="@android:drawable/ic_menu_manage">

        <!-- LSPosed / Xposed 模块声明 -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <!-- xposedminversion=93 + xposedsharedprefs：启用 LSPosed "New XSharedPreferences"，
             使模块 UI(MODE_WORLD_READABLE) 的勾选能被被 hook 进程跨进程读到。 -->
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedsharedprefs"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="kill vip: VIP/PRO解锁(每通道可勾选开关)+去广告+广告护栏 v1.12 仅作用域勾选App生效" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="kill vip">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
